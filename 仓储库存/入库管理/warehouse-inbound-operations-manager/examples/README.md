# 仓库入库作业经理场景示例

## 场景 1：采购或调拨入库

- 调用目标：使用 `warehouse-inbound-operations-manager` 完成该场景的完整业务结果，不只给原则。
- 必须提供：目标、范围、真实现状证据、owner、时间、权限和已知约束。
- 需要追问：现有数据/系统/模板/词库/规则、使用者优先级、不可改变项、下游接收方式和验收人。
- 预期交付：入库 SOP 与状态图、预约、收货、差异和上架任务表、SLA、证据、异常和对账清单。
- 故障注入：至少测试缺数据、口径冲突、权限不足或依赖失败中的一种，并给出暂停、补证、转人工或恢复路径。


## 场景 2：多仓预约和上架治理

- 调用目标：使用 `warehouse-inbound-operations-manager` 完成该场景的完整业务结果，不只给原则。
- 必须提供：目标、范围、真实现状证据、owner、时间、权限和已知约束。
- 需要追问：现有数据/系统/模板/词库/规则、使用者优先级、不可改变项、下游接收方式和验收人。
- 预期交付：入库 SOP 与状态图、预约、收货、差异和上架任务表、SLA、证据、异常和对账清单。
- 故障注入：至少测试缺数据、口径冲突、权限不足或依赖失败中的一种，并给出暂停、补证、转人工或恢复路径。


## 场景 3：短少、破损、错货和超收异常处理

- 调用目标：使用 `warehouse-inbound-operations-manager` 完成该场景的完整业务结果，不只给原则。
- 必须提供：目标、范围、真实现状证据、owner、时间、权限和已知约束。
- 需要追问：现有数据/系统/模板/词库/规则、使用者优先级、不可改变项、下游接收方式和验收人。
- 预期交付：入库 SOP 与状态图、预约、收货、差异和上架任务表、SLA、证据、异常和对账清单。
- 故障注入：至少测试缺数据、口径冲突、权限不足或依赖失败中的一种，并给出暂停、补证、转人工或恢复路径。


## 反例

- 只复述最佳实践，没有读取真实证据。
- 没有询问使用者已有资料、优先级和限制，就直接生成定稿。
- 只输出一张表或一段文案，没有上下游、异常、验收和责任闭环。
