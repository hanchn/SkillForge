# 数据库变更迁移规划师

> skill id：database-migration-planner
> 当前版本：1.0.4
> 产品状态：可用
> 所属分类：互联网研发 / 后端 / 数据库迁移

## 产品定位

数据库迁移是跨版本协议，不是一条能在本地执行成功的 ALTER 语句。

## 解决的问题

- 本地秒过的 ALTER 在生产长时间锁表
- 代码和 schema 发布顺序不兼容
- backfill 完成但数据未对账，旧列过早删除

## 适用场景

- 新增非空字段或约束
- 拆表、改类型、重命名或大规模 backfill
- 在线建立索引和迁移读写路径

## 合规与权限

- 合规配置：`product-engineering`（产研合规基线）。
- 执行前必须读取 `references/compliance-baseline.md`，完成司法辖区、数据、权限、授权、人工复核和审计检查。
- Skill 只能提供第一层规则约束，不能替代系统权限、真实授权、正式审批或专业法律意见。

## 不适用范围与边界

- 不得把向下迁移脚本等同于可恢复数据
- 不得在未知表规模和锁行为下直接执行生产 DDL
- 不得删除旧列、表或索引直到依赖和保留期验证完成
- 不得依赖不可重入的一次性 backfill

## 输入

- 当前/目标 schema 和数据语义
- 表规模、查询、流量、部署与数据库版本
- 应用、报表、任务和下游依赖

## 输出

- 分阶段迁移计划
- DDL/backfill 与兼容发布顺序
- 对账、监控和 gate
- 回滚/前滚恢复与清理计划

## 工作原理

1. 确认数据库、版本、表规模、流量、复制、备份、部署模型、SLO 和变更目标
2. 读取 schema、查询、ORM、任务、报表和下游依赖，建立读写影响图
3. 评估锁、表重写、索引构建、WAL/日志、复制延迟、磁盘和运行时间
4. 将不兼容变更拆成 expand、migrate、verify、switch 和 contract 阶段
5. 为 backfill 定义批次、顺序、节流、可重入、断点、失败重试和线上负载保护
6. 设计双读/双写或兼容代码时明确 source of truth、顺序、失败和退出条件
7. 建立行数、空值、唯一性、外键、汇总、抽样和业务不变量对账
8. 为约束和索引选择安全建立与验证顺序
9. 定义发布 gate、监控、暂停、回滚或前滚恢复以及备份恢复验证
10. 删除旧字段或路径前证明所有读取、写入、任务和下游已迁移

## 深度执行标准

- 覆盖完整业务理解、主场景与相邻变体、上下游、方案取舍、工具失败、异常、交付、验收和复盘。
- 复杂任务先读 `references/scenario-playbook.md`；存在决策或审批时使用 `assets/decision-record-template.md`。
- 不以篇幅代替深度，每项判断必须关联真实对象、证据、责任和结果。

## 技能包组成

- SKILL.md：AI 执行入口。
- references/migration-safety-checklist.md：专业检查表。
- assets/migration-runbook-template.md：交付模板。
- eval/acceptance.md：可判定验收。
- README.md：中文产品文档与版本记录。

## 使用方式

1. 提供目标、现有代码或生产证据与约束。
2. 让 AI 先读 SKILL.md 和指定 reference。
3. 按仓库实际命令验证，使用 asset 完成交付。

## 验收标准

- 生产规模、锁和运行时是否经过评估
- expand/migrate/contract 是否允许新旧版本共存
- backfill 是否可重入、可节流和可对账
- 恢复方案是否处理数据语义而不只回退代码

## 版本记录

| 版本 | 日期 | 更新内容 |
|---|---|---|
| 1.0.4 | 2026-07-15 | 自动检测到 Skill 实质内容升级并同步版本。 |
| 1.0.3 | 2026-07-15 | 自动检测到 Skill 实质内容升级并同步版本。 |
| 1.0.2 | 2026-07-15 | 自动检测到 Skill 实质内容升级并同步版本。 |
| 1.0.1 | 2026-07-15 | 自动检测到 Skill 实质内容升级并同步版本。 |
| 1.0.0 | 2026-07-15 | 建立首个可用的工程工作流、检查表、交付模板和验收标准。 |
